# Registry Schema Example - Simple Test
# This file demonstrates the registry schema which defines complete attribute 
# specifications including id, type, brief, examples, stability, etc.
# These attributes can then be referenced by entity files.

groups:
  - id: test.group
    brief: Test group
    attributes:
      - id: test.attr
        type: string
        brief: Test attribute
        examples: ["example1"]
